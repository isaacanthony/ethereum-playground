{
  "license": "MIT",
  "scripts": {
    "eslint": "eslint --no-error-on-unmatched-pattern **/",
    "ethlint": "solium --no-soliumignore -d **/contracts/",
    "jshint": "jshint **/migrations **/src **/test",
    "linter": "yarn run eslint && yarn run jshint && yarn run ethlint && yarn run solhint",
    "solhint": "solhint **/contracts/*.sol"
  },
  "dependencies": {
    "@chainlink/contracts": "0.3.0",
    "@openzeppelin/contracts": "4.4.1",
    "@truffle/hdwallet-provider": "2.0.0"
  }
}
